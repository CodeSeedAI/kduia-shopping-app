import React, { useContext } from 'react';

const Remaining = () => {
  return (
        <div className='alert alert-secondary'>
            <span>Remaining</span>

    </div>
    );
};

export default Remaining;